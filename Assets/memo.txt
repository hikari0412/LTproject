URP开启PostProcessing后render texture背景无法设置为透明
解决：
Packages/com.unity.render-pipelines.universal/Shaders/PostProcessing/UberPost.shader
将这个shader的
return half4(color, 1.0);
改为了
return half4(color, SAMPLE_TEXTURE2D_X(_SourceTexture, sampler_LinearClamp, SCREEN_COORD_REMOVE_SCALEBIAS(uv)).a);